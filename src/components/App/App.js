import React, { PropTypes, Component } from 'react';
import Board from '../Board';

var App = React.createClass({
  render: function() {
    return (
      <Board/>
    );
  }
});

export default App;
